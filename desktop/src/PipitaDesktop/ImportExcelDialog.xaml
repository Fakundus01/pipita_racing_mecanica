<Window x:Class="PipitaDesktop.ImportExcelDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Importar Excel"
        Width="580"
        Height="420"
        MinHeight="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F3F6FB"
        FontFamily="Bahnschrift">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#0F6FDE" />
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#095CC0" />
        <SolidColorBrush x:Key="SecondaryBrush" Color="#EEF4FB" />
        <SolidColorBrush x:Key="SecondaryHoverBrush" Color="#DFEAF8" />
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#203040" />
        </Style>
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="MinWidth" Value="118" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#1F2E43" />
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}" />
            <Setter Property="BorderBrush" Value="#D5E1EF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonRoot"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonRoot" Property="Background" Value="{StaticResource SecondaryHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Margin="16"
            Background="White"
            BorderBrush="#D8E2EF"
            BorderThickness="1"
            CornerRadius="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <TextBlock Text="Importar Excel"
                               FontSize="26"
                               FontWeight="Bold" />
                    <TextBlock Margin="0,8,0,18"
                               Foreground="#5D7287"
                               TextWrapping="Wrap"
                               Text="Elegi como queres aplicar los cambios del archivo sobre la base local." />

                    <Border Padding="12"
                            CornerRadius="12"
                            Background="#F7FAFE"
                            BorderBrush="#D8E2EF"
                            BorderThickness="1">
                        <StackPanel>
                            <TextBlock FontWeight="SemiBold" Text="Archivo seleccionado" />
                            <TextBlock x:Name="FilePathText" Margin="0,6,0,0" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Margin="0,18,0,0"
                            Padding="14"
                            CornerRadius="12"
                            Background="#FBFCFE"
                            BorderBrush="#D8E2EF"
                            BorderThickness="1">
                        <StackPanel>
                            <TextBlock Margin="0,0,0,10" FontWeight="SemiBold" Text="Modo de importacion" />

                            <RadioButton x:Name="MergeOption"
                                         IsChecked="True"
                                         Content="Merge recomendado"
                                         FontWeight="SemiBold" />
                            <TextBlock Margin="24,4,0,16"
                                       Foreground="#5D7287"
                                       TextWrapping="Wrap"
                                       Text="Actualiza filas con ID, crea filas nuevas y no elimina registros locales." />

                            <RadioButton x:Name="SincronizarOption"
                                         Content="Sincronizacion exacta"
                                         FontWeight="SemiBold" />
                            <TextBlock Margin="24,4,0,0"
                                       Foreground="#8E2430"
                                       TextWrapping="Wrap"
                                       Text="Ademas de actualizar y crear, elimina registros locales que ya no esten en las hojas presentes del Excel. Usar solo si el archivo refleja el estado final deseado." />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="1" Background="#F8FBFF" BorderBrush="#D8E2EF" BorderThickness="0,1,0,0" Padding="20,16">
                <DockPanel LastChildFill="False">
                    <Button DockPanel.Dock="Right" Style="{StaticResource DialogButtonStyle}" Content="Cancelar" Click="CancelarButton_Click" />
                    <Button DockPanel.Dock="Right" Style="{StaticResource PrimaryButtonStyle}" Content="Importar" Click="AceptarButton_Click" />
                </DockPanel>
            </Border>
        </Grid>
    </Border>
</Window>
