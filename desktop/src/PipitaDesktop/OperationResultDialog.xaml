<Window x:Class="PipitaDesktop.OperationResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="620"
        Height="470"
        MinHeight="470"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F3F6FB"
        FontFamily="Bahnschrift">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#0F6FDE" />
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="MinWidth" Value="118" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="16" Background="White" BorderBrush="#D8E2EF" BorderThickness="1" CornerRadius="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="22,20,22,0">
                <TextBlock x:Name="TitleText" FontSize="26" FontWeight="Bold" Foreground="#1D3550" />
                <TextBlock x:Name="SummaryText" Margin="0,8,0,0" Foreground="#5D7287" TextWrapping="Wrap" />
            </StackPanel>

            <Border Grid.Row="1" Margin="22,18,22,0" Padding="14" Background="#F7FAFE" BorderBrush="#D8E2EF" BorderThickness="1" CornerRadius="12">
                <TextBlock Text="Resumen detallado" FontWeight="SemiBold" Foreground="#203040" />
            </Border>

            <ScrollViewer Grid.Row="2" Margin="22,10,22,0" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="DetailText" TextWrapping="Wrap" Foreground="#203040" LineHeight="22" />
            </ScrollViewer>

            <Border Grid.Row="3" Background="#F8FBFF" BorderBrush="#D8E2EF" BorderThickness="0,1,0,0" Padding="20,16">
                <DockPanel LastChildFill="False">
                    <Button DockPanel.Dock="Right" Style="{StaticResource DialogButtonStyle}" Content="Cerrar" Click="CerrarButton_Click" />
                </DockPanel>
            </Border>
        </Grid>
    </Border>
</Window>
